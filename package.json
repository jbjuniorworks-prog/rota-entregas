{
  "name": "rota-entregas",
  "private": true,
  "type": "module",
  "scripts": {
    "servir": "http-server . -a 127.0.0.1 -p 8765 -c-1 -s",
    "planilhas": "node testes/gerar-planilhas.mjs",
    "motoristas": "node ferramentas/motoristas.mjs",
    "test": "playwright test --grep-invert @nuvem",
    "test:nuvem": "playwright test --grep @nuvem --workers 1"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "http-server": "14.1.1",
    "xlsx": "0.18.5"
  }
}
